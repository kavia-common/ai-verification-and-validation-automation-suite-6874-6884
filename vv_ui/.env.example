# PUBLIC_INTERFACE
# React app environment variables for vv_ui
# Copy this file to ".env" and adjust as needed for your environment.

# PUBLIC_INTERFACE
# Base URL for the backend API. Must include protocol and port.
# In local development, the backend runs on port 3001 by default.
REACT_APP_API_BASE_URL=http://localhost:3001
